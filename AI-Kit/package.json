{
  "name": "ai-kit",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "ts-node-dev --respawn --transpile-only --clear src/index.ts",
    "xxstart": "ts-node-dev --respawn --transpile-only src/index.ts",
    "xstart": "rimraf dist && tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@langchain/core": "^0.3.61",
    "@langchain/openai": "^0.5.15",
    "dotenv": "^16.5.0",
    "ts-morph": "^26.0.0"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/node": "^24.0.3",
    "chalk": "^4.1.2",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
